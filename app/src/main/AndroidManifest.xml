<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.orelandshadi.gamerfinder">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name=".ui.app.GamerFinderApp"
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.game.SettingsActivity"></activity>
        <activity android:name=".ui.game.AddFriendsActivity" />
        <activity android:name=".ui.game.ProfileActivity" />
        <activity android:name=".ui.game.ChatRoomActivity" />
        <activity android:name=".ui.game.SearchActivity" />
        <activity android:name=".ui.game.HomeActivity" />
        <activity android:name=".ui.game.FriendsActivity" />
        <activity android:name=".ui.login.ConfirmPasswordActivity" />
        <activity android:name=".ui.login.ConfirmEmailActivity" />
        <activity android:name=".ui.login.FavoriteGamesActivity" />
        <activity android:name=".ui.login.FavoritePlatformActivity" />
        <activity android:name=".ui.game.MainGamesActivity" />
        <activity android:name=".ui.login.DetailsActivity" />
        <activity android:name=".ui.login.SignUpActivity" />
        <activity android:name=".ui.start.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.login.LoginActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>